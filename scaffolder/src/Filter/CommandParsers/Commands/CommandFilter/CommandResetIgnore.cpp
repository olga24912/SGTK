#include <Filter/CommandParsers/State.h>
#include "CommandResetIgnore.h"

namespace filter {
    namespace commands {
        void CommandResetIgnore::execute(std::string argv, State &state, ContigGraph &graph) {
            //TODO: del command
        }
    }
}
